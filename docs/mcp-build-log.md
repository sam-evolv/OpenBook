# OpenBook MCP — Build Log

Track of every PR shipped against docs/mcp-server-spec.md.
Keep entries short. One line per PR. Newest at top.

## PRs
- PR #N: Migration C — business_media + new columns on businesses and bookings
- PR #N: Migration B — mcp_waitlist, mcp_promoted_slots, booking_feedback
- PR #N: Migration A — mcp_holds, mcp_tool_calls, mcp_query_log
