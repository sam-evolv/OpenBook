# components/dashboard-v2

Shared UI for the dashboard v2 redesign. See `docs/dashboard-v2-brief.md`.

Phase 1 will populate this folder with `ThemeProvider`, `Sidebar`, `TopBar`,
`Card`, `Button`, `Metric`, `FieldRow`, `MetricBlock`, `ContextRow` — the
production Tailwind equivalents of the v4 prototype primitives.

Keep the v5b.2 dashboard in `components/dashboard/` untouched until cutover.
